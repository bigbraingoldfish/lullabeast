[pytest]
# Avoid prepend import shadowing: autodev/tests imports put repo root on sys.path
# early, which breaks collection of sibling `tests/` modules (ModuleNotFoundError:
# tests.test_*). importlib mode keeps test module imports isolated.
addopts = --import-mode=importlib
pythonpath = .
