__pycache__/
*.pyc
*.pyo
.env
# Repo-local pipeline runtime (state, lock, queue, ideas, pipeline-project symlink)
.autodev/
# Local UI overrides (paths, hooks token) — copy from ui/config.example.json
ui/config.json
metrics.jsonl

# Local validation snapshots and ad-hoc UI-verify scaffolding (never committed)
.validation-backup/
tmp_ui_verify/

.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.log
.DS_Store
Thumbs.db
ui/index.html.bak
*.bak
# Suffixed backups (e.g. .env.bak-modelpins) that *.bak does not match.
*.bak-*

# Test-runner output (regenerated per run; never committed).
test-results/

# Accidental literal `~` directory (must never hold real OpenClaw data)
/~/

# IDE — do not commit local plans
.cursor/
/.vscode/

# code-review-graph — local graph DB, MCP wiring, and editor snippets (not part of release)
/.mcp.json
/.opencode.json
/.code-review-graph/
/AGENTS.md
/GEMINI.md
/.cursorrules
/.windsurfrules
# Claude Code tool config (like .vscode / .cursor — not part of the release)
/.claude/

# Local / internal planning (not for the public tree)
/plans/

*.egg-info/
dist/
build/
.venv/
venv/
node_modules/

# Pipeline metadata — orchestrator-managed per-turn state, never committed
*.done
phase_state.json
planner_output.json
executor_output.json
reviewer_output.json
escalation_output.json
failure_context.json
current_phase.json

# Tier A activity stamps (orchestrator/plugin runtime liveness markers)
*.stamp

# mkstemp atomic-write temp files (8-char hex suffix from tempfile.mkstemp)
pipeline_state_????????
phase_state_????????
current_phase_????????
DEFENSIVE-FINDINGS.md
PIPELINE_STATE_MACHINE_AUDIT.md
*-metaprompt.md
analyze_executor_sessions.py
executor-watchdog-calibration-findings.md


# Plugin build artifacts (compiled per-host by install.sh — never committed)
autodev/plugin/dist/
autodev/plugin/node_modules/

# Marketing site (local draft, not part of the release tree)
/Site/

# Internal queue-E2E validation rig (manual; references unpublished runbooks — kept local, not shipped)
scripts/queue-e2e-reset-test-projects.sh
scripts/queue-e2e-strict-freeze.sh
tests/test_freeze_script_portable.py
