"""Lullabeast MCP servers.

``dashboard_server`` exposes the UI dashboard's HTTP API as MCP tools so an
agent (Claude Code, an OpenClaw agent, any MCP client) gets full control of and
visibility into the pipeline. See ``autodev/mcp/README.md``.
"""
